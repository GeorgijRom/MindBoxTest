select a.product_name, b.category_name 
from products a
left outer join categories b on a.product_id = b.product_id

//я не до конца понял как именно должны называться 
столбцы в таблицах и сами таблицы соответственно, а так же что является ключами.
Поэтому предположил, что название таблиц: Products и category, а внешним ключом
по которому можно будет склеить будет product_id. Раз нам не важно есть ли у продукта
категория, значит должна выводиться вся таблица products, поэтому я использовал left outer join.
